/* designs algorithm that prints all permutatinos of a string*/


package main

import (
	"fmt"
	"strings"
)

func main() {
	str := "Hello World"
	perm(str)
}

func perm(str string) {
	var permutation = []string
	for i, val:= range str {
		
		
	}
}